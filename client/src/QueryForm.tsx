import React from "react";
import { Form, FormGroup, Label, Button } from "reactstrap";

function QueryForm() {
  return (
    <Form>
      <FormGroup>
        <Label />
        <Input />
      </FormGroup>
    </Form>
  );
};

export default QueryForm;
